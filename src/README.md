# mundial-supabase
utilizamos 

npx supabase login para el soporte de types luego

creas una carpeta en esa ruta que se llame types
npx supabase gen types typescript --project-id <TUID> > src/app/types/database.types.ts
